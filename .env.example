# JWT Secret Key (generate a strong secret key for production)
JWT_SECRET=your-secret-key-here-change-in-production

# Port (for local development)
PORT=3000

# Node Environment
NODE_ENV=development

# PostgreSQL Connection (for production - auto-injected by Vercel)
# POSTGRES_URL=postgresql://username:password@host:port/database
# DATABASE_URL=postgresql://username:password@host:port/database

# Note: For local development, SQLite will be used automatically
# For Vercel production, PostgreSQL will be used automatically
